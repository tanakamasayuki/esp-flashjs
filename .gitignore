node_modules

# Build output. Regenerated by npm run build / build:site / types.
dist
site
types

# arduino-cli build tree for the fixture provisioner. Per-chip, large, and
# reproducible from the sketch, so it is never worth committing.
tools/fixture-device/.build/

*.log
npm-debug.log*
.DS_Store

# Capture scratch. The region .bin files and MANIFEST.txt are the fixture;
# these are the machine-specific leftovers of producing them.
test/fixtures/hardware/*/capture.log
test/fixtures/hardware/*/flash.bin
test/fixtures/hardware/*/.chunk.bin
