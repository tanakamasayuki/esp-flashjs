# 変更履歴

[English](./CHANGELOG.md) · **日本語**

## 1.0.0 — 未リリース

実機で検証した最初のリリースです。このライブラリが解析する全形式を、ESP32 / ESP32-S3 / ESP32-P4 から吸い出したフラッシュに対して検証しており、プロトコル層も同じ3台を実際に駆動して確認しています。

### 0.1.0 をお使いの方へ

**0.1.0 は実機と会話できません。** 実機が手元に無い状態で公開したもので、5つの独立した不具合がそれぞれ単独でセッションを成立させなくしていました。どれもテストスイートを素通りしています。スイートが、ライブラリ自身の生成した fixture とモックに対して比較していたためです。

- **タイムアウトが自分の read を捨てていた。** 下層の read とタイマーを競走させており、タイマーが勝つと進行中のチャンクはキャンセルされずに放棄されます。以降のフレームが1つずつずれ、**全機種でチップ検出が失敗**する症状になりました
- **データコマンドのチェックサムがヘッダを含んでいた。** ROM は `0x07 Invalid CRC` を返します。対象はペイロードのみです
- **READ_FLASH の ack を SLIP で包んでいなかった。** stub は SLIP フレームを待つので、最初のブロックで転送が止まります
- **パーティションテーブルの magic がバイト逆順だった。** パーサは自分の出力しか受け付けず、実機のテーブルを不正と判定しました
- **otadata に標準 CRC-32 を使っていた。** ROM は seed 反転の規約を使うため、実機の otadata セクタはすべて壊れて見えました

### 追加

- **NVS の build と diff。** `buildNvs` はストアをパーティションイメージに書き戻し、**返す前に自分の出力を読み直して突き合わせます**。`diffNvs` は2つのイメージを比較し、リネーム検出も選べます
- **ファイルシステム。** `parseSpiffs` / `parseLittlefs` / `parseFat` がイメージからファイルを取り出します。ネストしたディレクトリ、多数の格納単位に跨るファイルにも対応。FAT は ESP-IDF の wear levelling 層越しに読みます
- **NVS・SPIFFS・LittleFS・FAT の analyzer。** 「未実装の形式」ではなく、認識して内容を記述するようになりました
- **チャンク分割＋チャンク単位のリトライ。** READ_FLASH の1転送は all-or-nothing なので、バイトを落とすリンクでは何度リトライしても大きな範囲は通りません。`read()` が範囲を分割して個別に再試行します
- **ファイルシステムの編集と再構築。** `FsStore` が編集可能なツリーを持ち、`buildFs` が SPIFFS / LittleFS / FAT のイメージとして**元と同じジオメトリで**書き戻します。各形式には、**ラウンドトリップでは誤魔化せない検査**を用意しました。SPIFFS はデバイスと同じくオブジェクトインデックス経由で読み戻し、LittleFS はブロックアロケータが辿る tail チェーンを歩き、FAT は wear levelling 状態を再生成せず引き継ぎます。`checkFsStore` は、構築前に「対象形式が持てないもの」を指摘します
- **Web アプリの通信速度セレクタ**、および inspector のファイルツリー（追加・置換・削除・書き戻し）・NVS エディタ・バイナリ差分ビュー
- **Analyzer と Transport の書き方のドキュメント。** どちらも動作する実例つきで、その実例はテストスイートが実行します
- **`tools/hardware-check.mjs`。** 実機に対してライブラリを走らせ、esptool のキャプチャと突き合わせます。`--rebuild` はさらに踏み込み、各ファイルシステムを編集して書き戻し、リセットして**チップ自身のドライバが何を見つけたか**を読み取ります。このライブラリが書いたイメージを実機がマウントすると言えるのは、この検査だけです

### 修正

- **ESP32-P4 で一切動作していなかった。** リビジョン v3.0 未満は RAM の配置が異なり専用の stub が必要ですが、通常の stub を存在しないアドレスに書いていたためチップは一度も挨拶を返しませんでした。**流通している P4 はすべて v3.0 未満**です。eFuse からリビジョンを読んで stub を選ぶようにしました
- **analyzer が消去済みフラッシュを掴んでいた。** テーブルから見れば真っ白な `nvs` も `nvs` なので、サブタイプヒントだけで未フォーマット領域を「空のファイルシステム」と報告していました。「何も保存されていない」と「一度も初期化されていない」は別物です
- **暗号化検出がチップの報告と矛盾していた。** 高エントロピーだけでは暗号文と圧縮データを区別できず、カウンタを含む**暗号化していない** LittleFS イメージが 8.0 bits/byte ちょうどを記録します。デバイス自身の報告を優先し、分からない場合はそう述べるようにしました
- **3機種中2機種で boot 領域を認識できなかった。** bootloader の開始位置はファミリごとに 0x0 / 0x1000 / 0x2000 と異なるのに、オフセット0しか見ていませんでした
- **パーティションテーブルの `encrypted` フラグを証拠として扱っていた。** これは方針ビットで、暗号化が無効なチップでは何の意味もありません
- **先頭のディレクトリエントリが長いファイル名だと FAT が空に見えた。** wear levelling の予備セクタは「ブートセクタが示すルートディレクトリ位置に実際のエントリが来る配置はどれか」で特定しますが、その判定が 8.3 名しか認めていませんでした。長いファイル名のエントリは UTF-16 なので検出自体が失敗し、「有効なレイアウトなし」になっていました
- **LittleFS のユーザ属性をタイプ完全一致で判定していた。** 実際のタイプは 0x300 にアプリケーションが選んだ1バイトを足したもので、どれも一致しません。ESP-IDF は更新日時を `'t'`（0x374）で保存しています

### 既知の制限

- **再構築は元のジオメトリでの再生成に限られます。** 任意パラメータでのフォーマットは対象外で、FAT は元イメージを必要とし、更新日時は引き継がれません
- FAT の長いファイル名は**コミット済み fixture ではカバーされていません**。プロビジョニングスケッチが書く名前に、長い名前を必要とするものが無いためです。代わりに `hardware-check.mjs --rebuild` が実機に対して確認します
- `fetchStub` はブラウザが必要です。モジュール隣の URL を `fetch()` に渡しますが、Node の fetch は `file:` を実装していません。ブラウザ外では `registerStub` を使ってください（[docs/transports.ja.md](./docs/transports.ja.md)）
- 暗号化領域の復号は[意図的に実装していません](./docs/spec.ja.md#95-復号-意図的に実装しない)
- 検証済みは ESP32 / ESP32-S3 / ESP32-P4 です。表にある他のチップは実装済みですが実機未検証で、README にそう明記してあります

## 0.1.0 — 2026-08-16

初回公開。Transport、ブートローダプロトコル、チップ検出、stub ロード、Flash の read / write / erase / verify / dump、パーティションテーブル、ファームウェアイメージ、OTA データ、バイナリ差分と検索、リファレンス Web アプリ。

実機が手元に無い状態で公開しました。上記の注記を参照してください。
